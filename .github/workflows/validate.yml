name: Validate Experimental Submission

on:
  pull_request:
  push:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate metadata and repository structure
        run: python3 scripts/validate_metadata.py
